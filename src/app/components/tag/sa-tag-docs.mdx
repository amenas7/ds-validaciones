import { Meta } from '@storybook/blocks';

<Meta title="Componentes/Legend/Legend Example" />

# 📚 Code Example - sa-legend

### HTML Template
```html
<div style="display: flex; justify-content: center; align-items: center; min-height: 120px;">
  <sa-tag
    [text]="tagText"
    [type]="tagType"
    [size]="tagSize"
    [class]="customClass">
  </sa-tag>
</div>

<div style="margin-top: 1.5rem; display: flex; justify-content: center; align-items: center; gap: 0.5rem; flex-wrap: wrap;">
  <button class="btn btn-sm btn-outline-primary" (click)="changeText()">
    Cambiar Texto
  </button>
  <button class="btn btn-sm btn-outline-info" (click)="changeType()">
    Cambiar Tipo ({{ tagType }})
  </button>
  <button class="btn btn-sm btn-outline-success" (click)="changeSize()">
    Cambiar Tamaño ({{ tagSize }})
  </button>
</div>
```

### ⚡ TypeScript Component

```typescript
import { Component } from '@angular/core';

@Component({
  ......
})

export class ........ {
// Propiedades dinámicas para el ejemplo
  tagText: string = 'Angular';
  tagType: 'primary' | 'secondary' | 'success' | 'danger' | 'warning' | 'info' | 'dark' | 'light' = 'success';
  tagSize: 'small' | 'medium' | 'large' = 'medium';
  customClass: string = '';

  // Arrays para rotación de valores
  private texts: string[] = ['Angular', 'TypeScript', 'JavaScript', 'Activo', 'Pendiente', 'Alta Prioridad', 'Frontend', 'Backend'];
  private types: ('primary' | 'secondary' | 'success' | 'danger' | 'warning' | 'info' | 'dark' | 'light')[] = [
    'primary', 'secondary', 'success', 'danger', 'warning', 'info', 'dark', 'light'
  ];
  private sizes: ('small' | 'medium' | 'large')[] = ['small', 'medium', 'large'];

  private currentTextIndex: number = 0;
  private currentTypeIndex: number = 2; // Inicia en 'success'
  private currentSizeIndex: number = 1; // Inicia en 'medium'

  // Métodos para cambiar propiedades dinámicamente
  changeText(): void {
    this.currentTextIndex = (this.currentTextIndex + 1) % this.texts.length;
    this.tagText = this.texts[this.currentTextIndex];
  }

  changeType(): void {
    this.currentTypeIndex = (this.currentTypeIndex + 1) % this.types.length;
    this.tagType = this.types[this.currentTypeIndex];
  }

  changeSize(): void {
    this.currentSizeIndex = (this.currentSizeIndex + 1) % this.sizes.length;
    this.tagSize = this.sizes[this.currentSizeIndex];
  }
}
```