import { Meta } from '@storybook/blocks';

<Meta title="Componentes/Icon/Icon Example" />

# 📚 Code Example - sa-icon

### HTML Template
```html
<sa-icon
  [name]="iconName"
  [color]="iconColor"
  [size]="iconSize">
</sa-icon>

<div class="mt-4">
  <button class="btn btn-sm btn-outline-primary me-2" (click)="changeIcon()">
    Cambiar Icono ({{ iconName }})
  </button>
  <button class="btn btn-sm btn-outline-info me-2" (click)="changeColor()">
    Cambiar Color ({{ iconColor }})
  </button>
  <button class="btn btn-sm btn-outline-success" (click)="changeSize()">
    Cambiar Tamaño ({{ iconSize }})
  </button>
</div>
```

### ⚡ TypeScript Component

```typescript
import { Component } from '@angular/core';

@Component({
  ......
})

export class ........ {
// Propiedades dinámicas para el ejemplo
  iconName: string = 'heart';
  iconColor: string = '#ff0000';
  iconSize: 'sm' | 'md' | 'lg' = 'md';

  // Arrays para rotación de valores
  private iconNames: string[] = ['heart', 'star', 'home', 'user', 'check', 'info', 'cog', 'bell'];
  private iconColors: string[] = ['#ff0000', '#ffd700', '#007bff', '#28a745', '#dc3545', '#17a2b8', '#6c757d', '#ffc107'];
  private iconSizes: ('sm' | 'md' | 'lg')[] = ['sm', 'md', 'lg'];

  private currentIconIndex: number = 0;
  private currentColorIndex: number = 0;
  private currentSizeIndex: number = 1; // Inicia en 'md'

  // Métodos para cambiar propiedades dinámicamente
  changeIcon(): void {
    this.currentIconIndex = (this.currentIconIndex + 1) % this.iconNames.length;
    this.iconName = this.iconNames[this.currentIconIndex];
  }

  changeColor(): void {
    this.currentColorIndex = (this.currentColorIndex + 1) % this.iconColors.length;
    this.iconColor = this.iconColors[this.currentColorIndex];
  }

  changeSize(): void {
    this.currentSizeIndex = (this.currentSizeIndex + 1) % this.iconSizes.length;
    this.iconSize = this.iconSizes[this.currentSizeIndex];
  }
}
```