import { Meta } from '@storybook/blocks';

<Meta title="Componentes/Legend/Legend Example" />

# 📚 Code Example - sa-legend

### HTML Template
```html
<div style="display: flex; justify-content: center; align-items: center; min-height: 200px;">
  <sa-legend
    [color]="legendColor"
    [tooltip]="legendTooltip"
    [tooltipPosition]="tooltipPosition"
    [class]="customClass">
  </sa-legend>
</div>

<div style="margin-top: 1.5rem; display: flex; justify-content: center; align-items: center; gap: 0.5rem; flex-wrap: wrap;">
  <button class="btn btn-sm btn-outline-primary" (click)="changeColor()">
    Cambiar Color
  </button>
  <button class="btn btn-sm btn-outline-info" (click)="changeTooltip()">
    Cambiar Mensaje Tooltip
  </button>
  <button class="btn btn-sm btn-outline-success" (click)="changeTooltipPosition()">
    Cambiar Posición Tooltip ({{ tooltipPosition }})
  </button>
</div>
```

### ⚡ TypeScript Component

```typescript
import { Component } from '@angular/core';

@Component({
  ......
})

export class ........ {
// Propiedades dinámicas para el ejemplo
  legendColor: string = '#28a745';
  legendTooltip: string = 'Este es un tooltip informativo que muestra detalles adicionales';
  tooltipPosition: 'top' | 'bottom' | 'left' | 'right' = 'top';
  customClass: string = '';

  // Arrays para rotación de valores
  private colors: string[] = ['#28a745', '#ffc107', '#dc3545', '#007bff', '#6f42c1', '#17a2b8', '#e83e8c', '#20c997'];
  private tooltips: string[] = [
    'Este es un tooltip informativo que muestra detalles adicionales',
    'Leyenda de ventas completadas - $543,210 en ingresos',
    'Productos en stock: 1,234 unidades disponibles',
    'Usuarios activos en los últimos 30 días',
    'Tooltip largo: Esta leyenda representa información estadística importante que requiere una explicación más detallada para el usuario',
    'Estado: Todo funcionando correctamente'
  ];
  private positions: ('top' | 'bottom' | 'left' | 'right')[] = ['top', 'bottom', 'left', 'right'];

  private currentColorIndex: number = 0;
  private currentTooltipIndex: number = 0;
  private currentPositionIndex: number = 0;

  // Métodos para cambiar propiedades dinámicamente
  changeColor(): void {
    this.currentColorIndex = (this.currentColorIndex + 1) % this.colors.length;
    this.legendColor = this.colors[this.currentColorIndex];
  }

  changeTooltip(): void {
    this.currentTooltipIndex = (this.currentTooltipIndex + 1) % this.tooltips.length;
    this.legendTooltip = this.tooltips[this.currentTooltipIndex];
  }

  changeTooltipPosition(): void {
    this.currentPositionIndex = (this.currentPositionIndex + 1) % this.positions.length;
    this.tooltipPosition = this.positions[this.currentPositionIndex];
  }
}
```